<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Transactions</title>
</head>
<body>
    <a href="/products">Products</a>
    <h1>List of Transactions</h1>
    <table border="1">
        <tr>
            <th>No</th>
            <th>Name</th>
            <th>Quantity</th>
            <th>Total Price</th>
            <th>Action</th>
        </tr>
    <% for (let i = 0; i < data.length; i++) { %>
        <tr>
            <td><%= i + 1%></td>
            <td><%= data[i].Product.name%></td>
            <td><%= data[i].quantity %></td>
            <td><%= data[i].formatRupiah(data[i].total_price) %></td>
            <td><a href="/transactions/delete/<%= data[i].id %>">Delete History</a></td>
        </tr>
    <%}%>
    </table>
</body>
</html>